# AI Library

本项目为一个集成了 AI 智能助手的现代化图书管理系统。项目采用前后端分离架构，前端使用 React + Vite，后端使用 Node.js + Express + PostgreSQL，并集成了 Ollama 本地大模型服务，提供智能图书推荐和 AI 问答助手功能。

## ✨ 主要功能

### 📚 图书管理

- **图书浏览与搜索**：支持多条件筛选和搜索图书。
- **借阅流程**：提供借书、还书、续借流程实现。
- **借阅记录**：用户可查看历史借阅记录和当前借阅状态。

### 🤖 AI 智能服务

- **智能助手**：基于 Ollama 与 OpenAI 的 AI 聊天助手，支持流式响应，可回答用户问题。
- **智能推荐**：根据用户借阅历史和偏好提供个性化图书推荐。

### 🛡️ 管理员后台

- **图书管理**：图书的增删改查，支持批量导入。
- **用户管理**：管理注册用户及其权限。
- **借阅管理**：查看和管理所有用户的借阅记录。
- **公告管理**：发布和管理系统公告。
- **数据统计**：可视化的借阅数据和用户活跃度统计。

### 👤 用户中心

- **个人资料**：管理个人信息和密码。
- **借阅概览**：个人借阅情况的仪表盘。

## 🛠️ 技术栈

- **前端**：React 19, Vite, Ant Design, Zustand, Axios, Recharts
- **后端**：Node.js, Express, PostgreSQL, Multer
- **AI/LLM**：Ollama, OpenAI
- **工具类**：Prettier, ESLint

## 🚀 快速开始

### 前置要求

- [Node.js](https://nodejs.org/) (>= 16.0.0)
- [PostgreSQL](https://www.postgresql.org/)
- [Ollama](https://ollama.com/)

### 1. 数据库设置

确保 PostgreSQL 服务已启动，并创建一个新的数据库（例如 `ai_library`）。相关的建表脚本请参考后端代码或自行初始化。

### 2. 后端启动

进入 `backend` 目录：

```bash
cd backend
```

安装依赖：

```bash
npm install
```

配置环境变量：
复制 `.env.example` 为 `.env` 或新建 `.env` 文件，填入以下配置：

```env
# 数据库配置
DB_USER=postgres
DB_HOST=localhost
DB_DATABASE=ai_library
DB_PASSWORD=your_password
DB_PORT=5432

# 会话配置
SESSION_SECRET=your_secret_key
SESSION_NAME=sid

# 允许跨域的源
ALLOWED_ORIGINS=http://localhost:5173

# Ollama 配置
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen3:1.7B
OLLAMA_TIMEOUT=60000
```

启动后端服务：

```bash
npm start
# 或者开发模式
npm run dev
```

### 3. 前端启动

进入 `frontend` 目录：

```bash
cd frontend
```

安装依赖：

```bash
npm install
```

启动开发服务器：

```bash
npm run dev
```

访问 http://localhost:5173 即可看到系统界面。

### 4. AI 服务设置

确保已安装 Ollama 并拉取了相应的模型（如 `qwen3:1.7B`）：

```bash
ollama pull qwen3:1.7B
ollama serve
```

## 📂 项目结构

```
AI-Library-Release/
├── backend/                # 后端服务
│   ├── src/
│   │   ├── routes/         # API 路由
│   │   ├── services/       # 业务逻辑
│   │   ├── utils/          # 工具函数
│   │   └── app.js          # 应用入口
│   ├── server.js           # 启动文件
│   └── ...
├── frontend/               # 前端应用
│   ├── src/
│   │   ├── components/     # 公共组件
│   │   ├── pages/          # 页面组件
│   │   ├── services/       # 请求封装
│   │   ├── stores/         # 状态管理
│   │   └── ...
│   └── ...
└── package.json
```
